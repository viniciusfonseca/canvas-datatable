{
  "name": "canvas-datatable",
  "license": "MIT",
  "version": "0.1.1",
  "description": "Small library with zero dependencies for datagrid rendering inside a canvas",
  "repository": {
    "url": "https://github.com/viniciusfonseca/canvas-datatable"
  },
  "main": "dist/index.js",
  "scripts": {
    "start": "parcel index.html --open",
    "build": "parcel build index.html",
    "prepublish": "rimraf dist && tsc && tsc --outDir dist/umd --module UMD"
  },
  "files": [
    "dist"
  ],
  "dependencies": {},
  "devDependencies": {
    "@babel/core": "7.2.0",
    "parcel-bundler": "^1.6.1",
    "rimraf": "^3.0.2",
    "typescript": "^3.7.5"
  },
  "keywords": [],
  "browserslist": [
    "last 1 Chrome versions"
  ]
}
